# GPU acceleration for the Qiskit VQA path — install AFTER requirements.txt:
#   .venv/bin/pip install -r requirements-gpu.txt
# Requires CUDA 12. qiskit-aer-gpu replaces qiskit-aer (same import name,
# GPU statevector enabled). 0.15.1 is the latest GPU build and requires
# qiskit<2.0 (requirements.txt pins qiskit==1.4.5 for exactly this reason —
# do not bump either without checking compatibility first).

qiskit-aer-gpu==0.15.1

# Transitive dependencies — cuQuantum / CUDA wheels
cudensitymat-cu12==0.5.2
cupauliprop-cu12==0.3.2
cuquantum-cu12==26.3.2
custabilizer-cu12==0.3.0
custatevec-cu12==1.13.1
cutensor-cu12==2.6.0
cutensornet-cu12==2.12.2
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cusolver-cu12==11.7.5.82
nvidia-cusparse-cu12==12.5.10.65
nvidia-nvjitlink-cu12==12.9.86
